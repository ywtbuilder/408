/*
题目：
一个长度为 L（L >= 1）的升序序列 S，处在第 (L / 2) 个位置的数称为 S 的中位数。
例如，若序列 S1 = (11, 13, 15, 17, 19)，则 S1 的中位数是 15。
两个序列的中位数是含它们所有元素的升序序列的中位数。
例如，若 S2 = (2, 4, 6, 8, 20)，则 S1 和 S2 的中位数是 11。

现在有两个等长升序序列 A 和 B，试设计一个在时间和空间两方面都尽可能高效的算法，
找出两个序列 A 和 B 的中位数。

要求：
1. 给出算法的基本设计思想。
2. 根据设计思想，采用 C、C++ 或 Java 语言描述算法，关键之处给出注释。
3. 说明所设计算法的时间复杂度和空间复杂度。
*/

#include <iostream>
using namespace std;

typedef int ElemType;

/*
（1）算法基本思想：
设两个等长升序序列的当前查找范围分别为 A[s1..d1] 和 B[s2..d2]。
每次分别求两个当前子序列的中位数 m1 和 m2：
1. 若 A[m1] == B[m2]，则该值就是两个序列的中位数。
2. 若 A[m1] < B[m2]，则最终中位数不可能在 A 的较小一半和 B 的较大一半中，
   因此保留 A 的后半部分和 B 的前半部分。
3. 若 A[m1] > B[m2]，则最终中位数不可能在 A 的较大一半和 B 的较小一半中，
   因此保留 A 的前半部分和 B 的后半部分。
不断缩小范围，直到两个子序列都只剩 1 个元素，此时较小者就是两个序列的中位数。
*/

/*
（2）算法实现：
*/
ElemType FindMedian(const ElemType A[], const ElemType B[], int n) {
    int s1 = 0, d1 = n - 1;
    int s2 = 0, d2 = n - 1;

    while (s1 != d1 || s2 != d2) {
        int m1 = (s1 + d1) / 2;
        int m2 = (s2 + d2) / 2;

        if (A[m1] == B[m2]) {
            return A[m1];
        }

        if (A[m1] < B[m2]) {
            // 当前长度为奇数时保留 A[m1]，当前长度为偶数时舍去 A[m1]
            if ((s1 + d1) % 2 == 0) {
                s1 = m1;
                d2 = m2;
            } else {
                s1 = m1 + 1;
                d2 = m2;
            }
        } else {
            // 与上面对称，保留 A 的前半部分和 B 的后半部分
            if ((s2 + d2) % 2 == 0) {
                d1 = m1;
                s2 = m2;
            } else {
                d1 = m1;
                s2 = m2 + 1;
            }
        }
    }

    return A[s1] < B[s2] ? A[s1] : B[s2];
}

/*
（3）复杂度分析：
时间复杂度：O(log n)。每次比较两个子序列的中位数后，查找范围约缩小一半。
空间复杂度：O(1)。算法只使用 s1、d1、s2、d2、m1、m2 等常数个辅助变量。
*/
